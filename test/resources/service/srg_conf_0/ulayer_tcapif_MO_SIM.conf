[ULAYER]
#
# TCAP Layer configuration
#
    FTOK_PATH       = $(USRG_DATA)/ipc/ulayer
    NAME            = TCAP
    TYPE            = IF
    ID              = 31
    HB_PERIOD       = 1000

    IPC_PORT        = $(USRG_KEYBASE)67

#
# it can contain multiple addresses
# when the current layer implementation is not working correctly
# the one on this path is used.
# can be empty
#
#IMPL_PATHS     = 127.0.0.1:$(USRG_KEYBASE)60#,100.100.1.1:$(USRG_KEYBASE)60
    IMPL_PATHS      = $(ULAYER_MY_IP):$(USRG_KEYBASE)60,$(ULAYER_PEER_IP):$(USRG_KEYBASE)60
    ENTITY_PATHS    = A-20-21-X,20-20-21-X,21-21-20-X

#
# user defined function to select the implementation
# to use per each msg.
#
#[ENTITY_SELECTOR]
#   DLL_PATH    = $(USRG_ROOT)/lib/libtcap_user.sl
#   FUNC_NAME   = selectEntity

#COLOCATED_IMPL = $(USRG_ROOT)/data/ulayer_tcap.conf
