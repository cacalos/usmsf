apiVersion: v1
kind: Service
metadata:
  namespace: shseo 
  name: usmsf-udm-sim
  labels:
    app: usmsf-udm-sim
spec:
  type: ClusterIP
  ports:
  - name: http-udmsim
    port: 8081
  - name: https-udmsim
    port: 9091
  selector:
    app: usmsf-udmsim-sim
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: usmsf-udm-v1
  namespace: shseo 
spec:
  replicas: 1
  selector:
    matchLabels:
     app: usmsf-udmsim-sim
  template:
    metadata:
      labels:
        app: usmsf-udmsim-sim
        version: v1
    spec:
      volumes:
      - name: shared-data
        emptyDir: {}
      - name: config-volume
        configMap:
          name: echo-usmsf-udmsim
      - name: log-volume
        emptyDir: {}
      - name: tcpdump 
        emptyDir: {}
#      hostAliases:
#      - ip: "192.168.7.180"
#        hostnames:
#        - "amf.uangel.com"
#        - "udm.uangel.com"
#      hostNetwork: true
#      dnsPolicy: ClusterFirstWithHostNet
      containers:
#      - name: tcpdump
#        image: docker.io/dockersec/tcpdump
#        command: ["sh", "-c", "--"]
#        args: ["while true; do sleep 30; done;"]
      - name: usmsf-udmsim
        image: camel.uangel.com:5000/usmsf-udm_sim:latest
        imagePullPolicy: Always
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
        - name: shared-data 
          mountPath: /tmp
        - name: log-volume
          mountPath: /app/logs
        ports:
        - containerPort: 8081
        - containerPort: 9091
        env:
        - name: USMSF_HOME
          value: /app
        - name: USMSF_CONFIG_FILE
          value: /app/config/reference.conf
#        - name: JAEGER_AGENT_HOST
#          valueFrom:
#            fieldRef:
#              fieldPath: status.hostIP
        command: ["sh", "-c", "--"]
        args: ["while true; do sleep 30; done;"]
---
